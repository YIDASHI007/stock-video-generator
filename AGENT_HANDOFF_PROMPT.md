# 给新电脑 AI Agent 的交接提示词

把下面整段提示词复制给新电脑上的 AI Agent，并把迁移 ZIP 文件路径一并提供给它。

---

你正在接手一个 Windows 本地项目“股票历史回测视频生成器”。请负责把迁移包安全恢复、安装、验证并启动，直到用户能在浏览器中正常使用。

迁移包路径由用户提供。请遵守以下要求：

1. 先读取压缩包旁边的 SHA256 文件并校验 ZIP；校验失败立即停止，不要解压。
2. 解压到用户指定目录；如果用户没有指定，使用空间充足的固定目录，例如 `D:\stock-video-generator`，不要放到 OneDrive、桌面临时目录或系统目录。
3. 完整阅读项目内的 `TRANSFER_GUIDE.md` 和 `README.md`。
4. 检查 Windows、Python 3.11+、Node.js 20+、Google Chrome、磁盘空间和网络。缺失依赖时，明确告诉用户需要安装什么；能在正常权限下安全安装的依赖可以直接安装。
5. 在项目根目录运行：

   ```powershell
   Set-ExecutionPolicy -Scope Process Bypass
   .\scripts\setup-transferred-project.ps1
   ```

6. 安装脚本会重建依赖、迁移数据库绝对路径、生成 `.env`、构建项目并运行测试。不要复制或复用旧电脑的 `.venv`、`node_modules` 或 Chrome 登录档案。
7. 如果安装脚本失败，先根据完整错误定位并修复环境问题，不要删除 `data`，不要清空 SQLite，不要重新初始化成空项目。
8. 安装完成后运行：

   ```powershell
   .\scripts\start.ps1
   ```

9. 验收：

   - `http://127.0.0.1:8877/health` 返回 `ok`；
   - `http://127.0.0.1:5173` 可以打开；
   - 历史成片数量与数据库一致；
   - 随机打开三条历史视频、横封面和竖封面；
   - Python 全量离线测试通过；
   - 前端和渲染器构建通过；
   - 新建一条测试回测时能获取真实行情并生成成片。

10. 迁移包故意不包含 `data/publish-accounts`。引导用户在发布中心重新扫码登录，但不要代替用户提交验证码。
11. 只做“预检并填写”验证发布页面、标题、话题和双封面。未经用户在本轮明确授权，不要点击正式发布，不要创建批量正式发布队列。
12. 自动生产在迁移快照中默认关闭。系统健康、测试成片和发布预检全部通过前，不要开启；通过后询问用户是否启用。
13. 不要让新旧两台电脑同时使用同一抖音账号自动发布。提醒用户确认新电脑稳定后，把它设为唯一生产与发布主机。

项目技术结构：

- FastAPI + SQLite 后端：端口 `8877`；
- React + Vite 前端：端口 `5173`；
- Remotion 渲染视频；
- Patchright 控制可见 Chrome 完成抖音创作者中心填写；
- 批量发布为持久化串行队列，发布间隔从上一条确认成功后开始计算；
- 抖音正式发布必须经过人工授权；
- 浏览器登录、短信验证和验证码不得由 Agent 绕过。

完成后向用户报告：

- 项目实际安装路径；
- Python、Node.js、pnpm 和 Chrome 版本；
- 健康检查结果；
- 测试与构建结果；
- 历史成片数量；
- 是否已经重新登录抖音；
- 自动生产是否仍关闭；
- 仍需用户完成的操作。

---

