# 社媒工作台 v0.1.12

## 产品定位与主界面

- 产品用户可见名称统一为“社媒工作台”，覆盖浏览器标题、侧边栏、启动中心、系统托盘、安装包标题和 GitHub Release 标题。
- 保留内部 PackId、可执行文件名和数据目录标识 `StockVideoGenerator`，确保已有安装可以原位升级。
- 运营总览新增“洞察 → 制作 → 管理 → 分发 → 复盘”工作路径，突出选题、内容生产、资产管理、多平台发布与运营复盘。
- 主界面优化桌面和移动端信息层级，首屏操作从单纯指标监控调整为任务导向。

## 启动中心

- 启动中心视觉与 Web 工作台统一为靛蓝、青绿色体系，并更新为社媒运营语境。
- 缩短检查步骤之间的等待动画和自动进入工作台的延迟，减少无效等待。
- 桌面快捷方式与安装包显示名称改为“社媒工作台”。

## 可读性与设计规范

- 新增统一的成功、信息、警告和错误提示组件，并补充 `aria-live`、`role` 和语义图标。
- 提高浅色和深色主题中的提示字号、字重、边框与对比度。
- 业务页面统一使用全局提示组件，新增自动设计规范检查，防止继续手写旧提示样式。
- 新增社媒工作台 UI 设计规范和新页面验收清单。

## 验证

- Web TypeScript 与设计规范检查通过。
- pnpm 全仓测试与构建通过。
- Python 离线测试通过。
- 桌面和 390×844 移动尺寸完成视觉复核。
